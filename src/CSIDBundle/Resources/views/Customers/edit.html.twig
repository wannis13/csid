{#{% if app.session.get('menu')=='mobilie-incendie'  %}#}
  {# {% extends 'base-mobilie-incendie.html.twig' %}#}
{#{% endif %}#}
{#{% if app.session.get('menu')=='signalletique-sur-mesure'  %}
	{% extends 'base.html.twig' %}
{% endif %}#}

{% set parent = 'base.html.twig' %}
 {% if app.session.get('menu') == 'mobilie-incendie'  %}
	{% set parent = 'base-mobilie-incendie.html.twig' %}
{% endif %}
{% extends parent %}
 {#{% if app.session.get('menu')=='mobilie-incendie'  %}
	 ********{{ app.session.get('menu') }}************
 {% endif %}

	 {% if app.session.get('menu')=='signalletique-sur-mesure'  %}
		 ---------{{ app.session.get('menu') }}--------------
	 {% endif %}#}
{% block fos_user_content %}

	<div class="margin-top container-shadow-top">
		<div class="container">
		
			<div class="title grid-full">
			    <h2>Clients <span>Modifier</span></h2>
			    <span class="border"></span>
			</div> 
		
			<div class="row">
				
				<div class="col-md-6">
				
					<form {{ form_enctype(form) }} method="POST" class="csid_customer_edit_form">
			    		
			    		<h2>Informations générales</h2>sdfdsfsd
			    		
			    		<div class="row">
						<div class="col-md-6">
							{{ form_row(form.email) }}
						</div>
						<div class="col-md-6">
							{{ form_row(form.username) }}
						</div>
					</div>
					
					<div class="row">
						<div class="col-md-6">
							{{ form_row(form.lastname) }}
						</div>

						<div class="col-md-6">
							{{ form_row(form.firstname) }}
						</div>

						
						<div class="col-md-6">
							{{ form_row(form.company) }}
						</div>
						
						<div class="col-md-6">
							{{ form_row(form.siren) }}
						</div>
						<div class="col-md-6">
							
						</div>
					</div>
					
					<h2>Adresse</h2>
					{{ form_row(form.address) }}
					<div class="row">
						<div class="col-md-6">
							{{ form_row(form.city) }}
						</div>
						<div class="col-md-6">
							{{ form_row(form.postal_code) }}
						</div>
					</div>
					
					<h2>Mot de passe</h2>
					<div class="row">
						<div class="col-md-6">
							{{ form_row(form.plainPassword.first) }}
						</div>
						<div class="col-md-6">
							{{ form_row(form.plainPassword.second) }}
						</div>
					</div>
						
						{{ form_row(form._token) }}
						
						<p>
					        <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" class="btn btn-primary" />
					    </p>
						{{ bootstrap_set_style('') }}
					    
					</form>
				</div>
			</div>
		</div>
	</div>
{% endblock %}

 